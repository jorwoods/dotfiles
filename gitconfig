[include]
        path = ~/.gitconfig_local
[core]
	editor = code --wait
[alias]
	a = add
	b = branch
	c = commit
	f = fetch --all --prune
	co = checkout
	cp = cherry-pick
	stat = status
	amend = !git commit --amend --no-edit
	pfwl = !git push --force-with-lease
